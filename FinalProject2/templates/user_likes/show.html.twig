{% extends 'base.html.twig' %}

{% block title %}UserLikes{% endblock %}

{% block body %}
    <h1>UserLikes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_like.id }}</td>
            </tr>
            <tr>
                <th>TypeName</th>
                <td>{{ user_like.TypeName }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_like.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_likes_index') }}">back to list</a>

    <a href="{{ path('app_user_likes_edit', {'id': user_like.id}) }}">edit</a>

    {{ include('user_likes/_delete_form.html.twig') }}
{% endblock %}
